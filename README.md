# Zamurasoft

Landing page para promocionar servicios de desarrollo de software.

## Cómo verla

Abre `index.html` en el navegador o sirve la carpeta con un servidor local:

```bash
npx --yes serve .
```

Luego visita la URL que imprima el comando (por lo general `http://localhost:3000`).
